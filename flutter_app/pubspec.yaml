name: kuyshim
description: "Kuyshim — Master the Dombra"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.4.0
  shared_preferences: ^2.5.0
  provider: ^6.1.0
  audioplayers: ^6.4.0
  google_fonts: ^6.2.1
  cached_network_image: ^3.4.1
  record: ^6.2.0
  permission_handler: ^12.0.1
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/lessons/
